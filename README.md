# canvas

Canvas is a multi-tenant BI platform designed for embedded analytics.

## Workspace

- `apps/backend`: API and worker runtime for the backend modular monolith.
- `apps/web`: tenant and operator web surfaces.
- `packages/*`: shared contracts, infra adapters, and common utilities.
- `infra/*`: deployment artifacts and environment definitions.
